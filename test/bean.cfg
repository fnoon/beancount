#!/usr/bin/env python3
#
# bean.cfg: Configuration file for Beancount "importers."
#           bean-extract uses this to ingest data from downloaded files.
#           bean-file uses this to move downloaded files to the documents/
#           directory.
#

# (In practice you might just change your PYTHONPATH environment.)
import sys
from os import path
sys.path.insert(0, path.join(path.dirname(__file__)))

from beancount.ingest import extract
from beancount.ingest.importers import ofx

# Setting this variable provides a list of importer instances.
CONFIG = [

    ofx.Importer("xxxxxx",	# Change for real account files,
				# but DON'T check in.
                 "Assets:Checking1",
                 "bofw_check1"),

    ofx.Importer("092243467384967",
                 "Assets:Checking2",
                 "bofw_check2"),
]

# Override the header on extracted text (if desired).
extract.HEADER = ';; -*- mode: org; mode: beancount; coding: utf-8; -*-\n'
